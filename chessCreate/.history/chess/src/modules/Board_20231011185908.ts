import { Cell } from "./Cell"

export class Board {
    private cells: Cell[][] = [] 

    public initCells(){
        for (let index = 0; index < array.length; index++) {
            const element = array[index];
            
        }
    }
}