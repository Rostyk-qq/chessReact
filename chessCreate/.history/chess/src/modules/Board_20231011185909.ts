import { Cell } from "./Cell"

export class Board {
    private cells: Cell[][] = [] 

    public initCells(){
        for (let i = 0; i < array.length; i++) {
            const element = array[i];
            
        }
    }
}