import { Cell } from "./Cell"

export class Board {
    private cells: Cell[][] = [] 

    public initCells(){
        for (let i = 0; i < 8; i++) {
            const row: Cell[] = [];
            for (let j = 0; j < array.length; j++) {
                const element = array[j];
                
            }
            
        }
    }
}